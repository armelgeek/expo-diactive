export class User {
    constructor(id, email, username, fullName, phone) {
      this.id = id;
      this.email = email;
      this.username = username;
      this.fullName = fullName;
      this.phone = phone;
    }
  }